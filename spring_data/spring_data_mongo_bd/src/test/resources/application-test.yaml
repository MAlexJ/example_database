de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5
        storage:
          repl-set-name: rs0
spring:
  data:
    mongodb:
      database: test

logging:
  level:
    org:
      mongodb: off
      springframework:
        boot:
          autoconfigure:
            mongo:
              embedded: off