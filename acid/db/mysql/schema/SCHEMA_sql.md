To create a schema in MySQL, you can use the following SQL command:

```sql
CREATE SCHEMA test_db
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

To verify if the schema was created successfully, you can list all schemas using:

```sql
SHOW DATABASES;
```

